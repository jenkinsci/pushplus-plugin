<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout"
         xmlns:t="/lib/hudson" xmlns:f="/lib/form">
    <f:section title="配置 pushplus 账号信息">
        <f:entry title="Jenkins URL" field="returnUrl" value="${descriptor.returnUrl}">
            <f:textbox/>
        </f:entry>
        <f:entry title="用户token或消息token" field="tokenId" value="${descriptor.tokenId}">
            <f:textbox/>
        </f:entry>
    </f:section>
</j:jelly>